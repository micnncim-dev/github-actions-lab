name: Sync labels

on:
  push:
    paths:
      - '.github/labels.yml'
jobs:
  build:
    name: Sync labels
    runs-on: ubuntu-latest
    steps:
      - uses: micnncim/action-label-syncer@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

